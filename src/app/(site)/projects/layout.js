import { Fragment } from "react"

export const metadata = {
    title: 'Our Projects- NGO',
    description: 'Helping the society to grow',
  }
  
  export default function ProjectLayout({ children }) {
    return (
      <Fragment>
          {children}
      </Fragment>
    )
  }
  
  
  