import { Fragment } from "react"

export const metadata = {
  title: 'Contact us - NGO',
  description: 'Reach out to us',
}

export default function ContactLayout({ children }) {
  return (
    <Fragment>
        {children}
    </Fragment>
  )
}


